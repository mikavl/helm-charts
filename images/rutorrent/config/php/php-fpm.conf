[global]
pid = /run/rutorrent/php8.2-fpm.pid
error_log = /dev/stderr
php_value[session.save_path] = /run/php/session

[rutorrent]
listen = /run/rutorrent/php8.2-fpm.sock
listen.owner = rutorrent
listen.group = rutorrent
pm = static
pm.max_children = 5
pm.start_servers = 5
